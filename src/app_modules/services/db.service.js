class DBService {
    blockGroups = require('../../instances/block-group');
    blockTypes  = require('../../instances/block-type');
    mapTypes    = require('../../instances/map-type');
}

module.exports = new DBService();
